query GetRankings($page: Int!, $size: Int!) {
  allRankings(page: $page, size: $size) {
    code
    data {
      id
      level
      position
    }
    totalPages
    totalItems
    currentPage
    message
  }
}
