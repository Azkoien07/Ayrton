query GetPayments($page: Int!, $size: Int!) {
  allPayments(page: $page, size: $size) {
    code
    data {
      id
      purchaseAmount
      paymentMethod
    }
    totalPages
    totalItems
    currentPage
    message
  }
}
