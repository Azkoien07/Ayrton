query GetChallenges($page: Int!, $size: Int!) {
  allChallenges(page: $page, size: $size) {
    code
    data {
      id
      name
      description
      category
      state
      dificulty
    }
    totalPages
    totalItems
    currentPage
    message
  }
}
