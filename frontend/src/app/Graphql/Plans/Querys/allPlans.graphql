query GetPlans($page: Int!, $size: Int!) {
  allPlans(page: $page, size: $size) {
    code
    data {
      id
      name
      description
      price
      state
      duration
    }
    totalPages
    totalItems
    currentPage
    message
  }
}
