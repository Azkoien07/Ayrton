query GetPqrs($page: Int!, $size: Int!) {
  allPqrs(page: $page, size: $size) {
    code
    data {
      id
      typePqr
      title
      description
      argument
      answer
      state
    }
    totalPages
    totalItems
    currentPage
    message
  }
}
